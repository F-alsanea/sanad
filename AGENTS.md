# AGENTS.md — قواعد العمل في هذا المستودع

> ملف مُلزِم لأي وكيل يعمل هنا. اقرأه كاملاً في بداية كل جلسة.

## قاعدة الأسبقية

عند التعارض: هذا الملف يفوز في النطاق والمراحل وانتظار `نفّذ` والبنية المؤجلة.
FABLE يفوز في fail-closed وصفر placeholders واقرأ-قبل-اكتب والدليل قبل «تم».

## جوهر FABLE — دائم

- اقرأ الملف قبل تعديله.
- لا كود قبل التقرير و`نفّذ` إلا إذا أمر المستخدم بالتنفيذ مباشرة.
- لا TODO / stub في مسار إنتاج.
- لا تدّعي تحقق بلا أثر (ملف:سطر أو مخرج أمر).
- أمن البيانات fail-closed حتى لو البنية مؤجلة.

## استقبال الجلسة

1. افحص المستودع بلا تعديل.
2. صنّف NEW أو EXISTING.
3. اكتب خريطة الجلسة وقف.
4. لا كود قبل `نفّذ`.

```
SESSION
- نوع المشروع: NEW / EXISTING
- التقنيات (مشاهَدة):
- ما يعمل الآن:
- المهمة الحالية:
- طبقات FABLE المطلوبة:
- المؤجل:
- السؤال المفتوح (واحد أو صفر):
```

## القواعد الحاكمة

1. ممنوع إعادة كتابة ملف كامل.
2. ممنوع تعديل ملف غير مذكور.
3. ممنوع تغيير الشكل إلا إذا كان هو الإصلاح.
4. ممنوع حذف ميزة أو ملف بلا تأكيد.
5. ممنوع إضافة مكتبة بلا إذن.
6. الناقص = اسأل.
7. مرحلة واحدة في كل رد، ثم انتظر.
8. تقرير أولاً. لا تنفيذ حتى `نفّذ`.
9. بعد التنفيذ: ملف:سطر + قبل/بعد + اختبار يدوي.
10. الصرامة على قدر الخطر.

## مهارات مرفقة

- `skills/fable` — تحميل طبقي لـ FABLE.
- `skills/app-security-gate` — إلزامي قبل الشحن وعند API/أدمن/بيانات.

**ابدأ بالمرحلة 0. لا تكتب كوداً قبل موافقة المستخدم.**
